import * as actions from /* IMPORT ACTION */;

const initialState = {
    /* INITIAL STATE */
};

export default (state=initialState, action) => {
    if (action.type === actions.ACTION_CONSTANT_A) {
      // CREATE NEW OBJECT WITH OBJECT ASSIGN OR OBJECT SPREAD
      // UPDATE VALUES AND RETURN
    }
    else if (action.type === actions.ACTION_CONSTANT_A) {
      // CREATE NEW OBJECT WITH OBJECT ASSIGN OR OBJECT SPREAD
      // UPDATE VALUES AND RETURN
    }
    else if (action.type === actions.ACTION_CONSTANT_A) {
      // CREATE NEW OBJECT WITH OBJECT ASSIGN OR OBJECT SPREAD
      // UPDATE VALUES AND RETURN
    }


    return state; // if no match, return original state
};
